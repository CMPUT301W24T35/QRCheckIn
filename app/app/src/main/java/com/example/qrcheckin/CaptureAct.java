package com.example.qrcheckin;

import android.os.Bundle;
import android.view.View;
import android.widget.Button;
import android.widget.ImageButton;

import com.journeyapps.barcodescanner.CaptureActivity;

/**
* This class is for capturing activities
*/
public class CaptureAct extends CaptureActivity {
    /*
    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.capture_view); // Use the custom layout file you created

        ImageButton closeButton = findViewById(R.id.close_button);
        closeButton.setOnClickListener(v -> finish()); // Finish the activity when the close button is tapped
    }

     */
}
